'use strict';

angular.module('freexf')
  .controller('starStudent_ctrl', function ($scope, $rootScope, $injector, $state, $ionicLoading, $ionicModal, $fxModal, AUTH, ENV, DispatchRepository) {
      

  });
